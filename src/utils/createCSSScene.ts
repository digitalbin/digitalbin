import * as THREE from 'three';

export default function createScene() {
    const cssScene = new THREE.Scene();
    // cssScene.scale.set(.05, .05, .05);
    // cssScene.background = new THREE.Color(0xffffff);
    return cssScene;
}
